from enum import Enum, auto

class Anchor(Enum):
	UPPER_LEFT = auto(),
	UPPER_CENTER = auto(),
	UPPER_RIGHT = auto(),
	
	LEFT = auto(),
	CENTER = auto(),
	RIGHT = auto(),
	
	LOWER_LEFT = auto(),
	LOWER_CENTER = auto(),
	LOWER_RIGHT = auto(),